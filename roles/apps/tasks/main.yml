# -- INCLUDE OS SPECIFIC TASKS -- #
# - name: "include macos tasks"
#   include_tasks: osx.yml
#   when: ansible_facts['os_family'] == "Darwin"
#
# - name: "include fedora tasks"
#   include_tasks: fedora.yml
#   when: ansible_facts['os_family'] == "RedHat"

# -- INCLUDE OS SPECIFIC TASKS -- #
# - name: "include macos tasks"
#   include_tasks: "{{ item }}"
#   loop: "{{ query('fileglob', 'osx/*.yml') | sort }}"
#   when: ansible_facts['os_family'] == "Darwin"
#
# - name: "include linux tasks"
#   include_tasks: "{{ item }}"
#   loop: "{{ query('fileglob', 'linux/*.yml') | sort }}"
#   when: ansible_facts['os_family'] == "RedHat"

- name: "include {{ onesetup_os }} tasks"
  include_tasks: "{{ onesetup_os }}.yml"

# -- -- -- -- -- -- -- -- #
# --     INVENTORY     -- #
# -- -- -- -- -- -- -- -- #

# -- DEFAULT: variables -- #
# note: per host variables are set in host_vars/<hostname>.yml
[all.vars]
ansible_connection = "ssh"
ansible_port = 22
ansible_user = "{{ su_username }}"
ansible_become_password = "{{ su_password }}"
ansible_password = "{{ su_password }}"

# -- WORKSTATION -- #
# hosts
[workstation.hosts]
onexmac-cratos = { ansible_host = "100.65.55.179" }

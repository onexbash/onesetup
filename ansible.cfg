[defaults]
become = true # execute with escalated sudo privileges
# home = /opt/onesetup # root path for ansible config files on the controller
inventory = inventory/hosts.yml
host_key_checking = False # whether to refuse the ssh connection for hosts that are not in the known_hosts file
result_format = yaml

[privilege_escalation]
become_user = fschlegelone
